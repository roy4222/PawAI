# ============================================================
# config/school_demo.env.example — Demo 環境設定範例
# ============================================================
#
# 開源化用：clone repo 後複製成 school_demo.env、填入自家網段，即可跑：
#   cp config/school_demo.env.example config/school_demo.env
#   vim config/school_demo.env       # 填 JETSON_IP / GATEWAY_HOST 等
#   JETSON_IP=192.168.1.42 source config/school_demo.env
#
# 必填：JETSON_IP 或 GATEWAY_HOST 至少一個
# 其餘有合理 default，學校現場通常不用改
# ============================================================

# === 必填 ===

# 法 A：學校 Jetson 區網 IP（Mac 與 Jetson 同網段）
# JETSON_IP=192.168.1.42

# 法 B：Jetson hostname / mDNS / Tailscale 名稱
# GATEWAY_HOST=jetson-school

# === 通常不用改 ===

# Go2 機體 IP（Ethernet 直連預設）
# ROBOT_IP=192.168.123.161

# ROS2 multi-machine 隔離（同實驗室多人時改）
# ROS_DOMAIN_ID=0

# Studio gateway port
# GATEWAY_PORT=8080

# Foxglove WebSocket port
# FOXGLOVE_PORT=8765

# === 雲端服務 endpoint（Jetson-side only — Mac source 不影響）===
#
# 預設假設 Jetson 端有 SSH tunnel 到雲端：localhost:8000 / 127.0.0.1:8001
# 學校無 tunnel 時先設 LLM_HOST / ASR_HOST 指向實際 host 即可：

# 法 A：tunnel 模式（預設）— 不用設
# 法 B：直連雲端
# LLM_HOST=10.0.0.5   # 雲端 vLLM IP
# ASR_HOST=10.0.0.5   # 雲端 SenseVoice IP

# 完全自定 endpoint（覆寫上面的 *_HOST + port 組合）
# LLM_ENDPOINT=http://your-llm:8000/v1/chat/completions
# QWEN_ASR_BASE_URL=http://your-asr:8001/v1/audio/transcriptions

# === 路徑（學校 Jetson 不同就改）===

# 學校場地地圖檔（5/13 場勘建圖後填入）
# MAP=/home/jetson/maps/school_demo.yaml
# MAP_DIR=/home/jetson/maps

# repo 路徑（cloned 到 ~/elder_and_dog 不用改）
# WORKDIR=$HOME/elder_and_dog

# 模型路徑（Jetson 標配 /home/jetson/models 不用改）
# MODEL_BASE=/home/jetson/models
