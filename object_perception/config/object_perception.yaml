object_perception_node:
  ros__parameters:
    model_path: "/home/jetson/models/yolo26n.onnx"
    trt_cache_dir: "/home/jetson/trt_cache/"
    color_topic: "/camera/camera/color/image_raw"
    confidence_threshold: 0.5
    input_size: 640
    tick_period: 0.067
    publish_fps: 8.0
    class_cooldown_sec: 5.0
    # Class whitelist: declared default is [] (all 80 COCO classes).
    # To restrict, uncomment and set explicitly (see object_perception/coco_classes.py).
    # Common P0 subset:
    # class_whitelist: [0, 16, 39, 41, 56, 60]
    # = [person, dog, bottle, cup, chair, dining_table]
