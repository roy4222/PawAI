## ROS2 build artifacts
build/
log/
install/

## IDE / OS
.vscode/
.DS_Store

## Python
__pycache__/
*.pyc
*.egg-info/
.coverage
coverage.xml
.ruff_cache/
.pytest_cache/

## Environment
.env
.env.local
.env.*.local
.venv*/

## ROS2 runtime logs
.ros_logs/

## Data files (should not be in repo)
*.ply
tts_cache/

## Claude Code
.claude/test_results/*.csv
.claude/settings.local.json
.claude/skills/
!.claude/skills/jetson-verify/
!.claude/skills/project-onboard/
!.claude/skills/update-docs/
!.claude/skills/jetson-deploy/
!.claude/skills/jetson-status/
!.claude/skills/jetson-verify/

## Package-level coverage (legacy paths)
go2_robot_sdk/.coverage
go2_robot_sdk/coverage.xml

## Test outputs
logs/jetson-verify/*.json
logs/*.jsonl
test_results/*.json

## Docs
PROJECT_MAP.md

## Benchmark raw results (working area)
benchmarks/results/raw/*.jsonl

## LLM eval raw results (intermediate artefacts; summary lives in result doc)
tools/llm_eval/results/*.json

## Node.js (pawai-studio has its own, root should not)
node_modules/

## Codex CLI marker file
.codex

## Agent runtime artefacts（自動產生）
/.agents/

## 會議錄音 / datasheet PDF（保留在硬碟但不入 repo）
/*.m4a
/*.mp3
/*.wav
/LD*.pdf

## Windows / WSL 下載 metadata（每個檔案會帶一份）
*Zone.Identifier
*:Zone.Identifier

## Repo root 的暫存截圖 / 參考圖（reference 用，不入 commit）
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.gif
/螢幕擷取畫面*
/LM*.pdf
*.py[cod]
