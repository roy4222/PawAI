# 今日開發計畫 - 2025/12/08 (日)

**目標：** W6 完結 + 修復 Go2 Driver 連線 + 驗證 Nav2 導航  
**預估時間：** 1.5 - 2 小時  
**關鍵里程碑：** 機器狗成功導航移動

---

## 📊 專案進度概覽

| 週次                   | 目標                   | 狀態       |
| ---------------------- | ---------------------- | ---------- |
| **W6** (12/2-12/8)     | MCP 基礎串接           | ⏳ 今日完結 |
| **W7** (12/9-12/15)    | 視覺閉環 + Action 驗證 | ⏳ 明日開始 |
| **W8** (12/16-12/22)   | 尋物情境整合           | ⏳          |
| **W9-W10** (12/23-1/6) | Demo 準備              | ⏳          |

---

## ⏰ 今日時間安排

| 時段 | 任務 | 預估時間 | 成功標準 |
|------|------|----------|----------|
| **Phase 1** | 修復 Go2 Driver 連線 | 30-60 分鐘 | `/scan` ~5 Hz, TF Tree 正常 |
| **Phase 2** | 驗證 Nav2 導航 | 30 分鐘 | 機器狗成功向前移動 |
| **Phase 3** | W6 收尾與文件更新 | 15 分鐘 | 文件標記 W6 ✅ |
| **Phase 4** | W7 預備（可選） | 15 分鐘 | 整理明日任務清單 |

---

## 🚨 昨日遺留問題 (Current Blocker)

### Go2 Driver 連線失敗 (DTLS Error)

**現象：**
- `zsh phase1_test.sh t1` 時出現 `DTLS handshake failed (connection error)`
- `/scan` 和 `/tf` Topic 無數據
- TF Tree 為空

**影響：**
- Nav2 無法運作（沒有感測器資料）
- 所有參數調整都已完成，但車子「熄火中」

**根因推測：**
1. Go2 機器狗的 WebRTC Server 狀態異常
2. 網路配置不穩定
3. DTLS Certificate 過期或不匹配

---

## 🎯 今日任務

### Phase 1：修復 Go2 Driver 連線（🔴 最高優先）

| 步驟 | 任務            | 驗收標準                                   |
| ---- | --------------- | ------------------------------------------ |
| 1.1  | 重啟 Go2 機器狗 | 機器狗完全開機                             |
| 1.2  | 執行環境檢查    | `zsh phase1_test.sh env` 雙通測試通過      |
| 1.3  | 啟動 Driver     | `zsh phase1_test.sh t1` 看到 `Connected`   |
| 1.4  | 驗證感測器      | `ros2 topic hz /scan` 顯示 ~5 Hz           |
| 1.5  | 檢查 TF Tree    | `ros2 run tf2_tools view_frames` 有完整 TF |

**如果連線仍失敗：**
- 檢查 Go2 的 WebRTC Server Log
- 嘗試重設 VM 網路（`sudo ip addr flush dev enp0s1`）
- 考慮使用乙太網（Ethernet）模式而非 WebRTC

---

### Phase 2：驗證 Nav2 導航（連線修復後）

| 步驟 | 任務             | 驗收標準                                                           |
| ---- | ---------------- | ------------------------------------------------------------------ |
| 2.1  | 啟動 SLAM + Nav2 | `zsh phase1_test.sh t3` 成功                                       |
| 2.2  | 驗證參數生效     | `ros2 param get /controller_server FollowPath.max_vel_theta` = 1.0 |
| 2.3  | 發送導航目標     | `ros2 action send_goal /navigate_to_pose ...` 機器狗移動           |
| 2.4  | 監控 /cmd_vel    | `linear.x > 0`，`angular.z <= 1.0`                                 |

**已完成的 Nav2 參數修正（不要重複改！）：**

| 參數                          | 值  | 目的         |
| ----------------------------- | --- | ------------ |
| `max_vel_theta`               | 1.0 | 避免瘋狂旋轉 |
| `min_vel_x`                   | 0.1 | 強制前進     |
| `global_costmap.global_frame` | map | 修正座標系   |
| `min_obstacle_height`         | 0.1 | 過濾地板反射 |

---

### Phase 3：W6 收尾與文件更新

| 任務                            | 驗收標準             |
| ------------------------------- | -------------------- |
| 更新 `開發計畫.md` 標記 W6 完成 | W6 狀態改為 ✅        |
| 整理今日筆記                    | 記錄成功/失敗項目    |
| 準備 W7 預備工作                | 列出明日 (12/9) 任務 |

---

## ⏰ 建議時間安排

| 時段 | 任務               | 預估時間   |
| ---- | ------------------ | ---------- |
| 開始 | Phase 1：修復連線  | 30-60 分鐘 |
| 中段 | Phase 2：驗證 Nav2 | 30 分鐘    |
| 尾段 | Phase 3：文件更新  | 15 分鐘    |

---

## 📋 快速指令摘要

### 環境檢查與 Driver 啟動

```bash
# Terminal 1: 環境檢查
cd ~/ros2_ws/src/elder_and_dog
zsh phase1_test.sh env

# Terminal 2: 啟動 Driver（必須透過 phase1_test.sh）
zsh phase1_test.sh t1
# 成功標準：看到 [go2_driver_node]: Connected

# Terminal 3: 驗證感測器
source ~/ros2_ws/install/setup.zsh
ros2 topic hz /scan  # 應該 ~5 Hz
```

### Nav2 測試

```bash
# Terminal 2: 啟動 SLAM + Nav2（Driver 成功後）
zsh phase1_test.sh t3

# Terminal 4: 驗證參數
source ~/ros2_ws/install/setup.zsh
ros2 param get /controller_server FollowPath.max_vel_theta
# 必須看到 1.0

# Terminal 4: 發送導航目標
ros2 action send_goal /navigate_to_pose nav2_msgs/action/NavigateToPose \
  "{pose: {header: {frame_id: 'map'}, pose: {position: {x: 0.5, y: 0.0, z: 0.0}, orientation: {w: 1.0}}}}" \
  --feedback
```

---

## 💡 昨日教訓總結

### 1️⃣ 不要跳過 Driver 驗證直接測試 Nav2

**錯誤模式：**
```
直接跑 t3 → Nav2 卡住 → 花 2 小時調參數 → 發現 Driver 根本沒連上
```

**正確模式：**
```
跑 t1 → 確認 /scan 有數據 → 跑 t3 → Nav2 正常工作
```

### 2️⃣ 環境變數必須一致

確保每個 Terminal 都執行：
```bash
source ~/ros2_ws/install/setup.zsh
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
export CYCLONEDDS_URI=/home/roy422/local_only_v2.xml
```

或使用 `zsh phase1_test.sh t*` 自動載入。

### 3️⃣ 不要混用 SLAM/Nav2 模式與 MCP 模式

| 模式      | 指令組合                 | 用途           |
| --------- | ------------------------ | -------------- |
| SLAM/Nav2 | `env` → `t1` → `t3`      | 完整導航測試   |
| MCP       | `env` → `bridge` → `mcp` | Kilo Code 控制 |

**不要同時運行兩種模式！**

### 4️⃣ colcon build 後必須 source

每次 `colcon build` 後：
```bash
source install/setup.zsh
```
否則修改不會生效。

---

## 📊 今日成功標準

- [ ] Go2 Driver 連線成功（`/scan` 有數據）
- [ ] Nav2 導航成功（機器狗向前移動）
- [ ] 更新 `開發計畫.md` 標記 W6 完成
- [ ] 筆記區記錄今日成果

---

## 📚 今日學習：TF 除錯歷史分析

**時間：** 15:30 - 16:00（機器狗不在身邊的空檔）

### 🔍 分析過往 dev_notes 的 TF 除錯記錄

| 日期      | TF 狀態 | 使用的工具                | 結論                          |
| --------- | ------- | ------------------------- | ----------------------------- |
| **11/24** | ✅ 正常  | `view_frames`, `tf2_echo` | 首次學習 TF 工具              |
| **12/01** | ✅ 正常  | `view_frames`             | 確認使用方式                  |
| **12/02** | ✅ 正常  | 驗證 TF Tree              | `map → odom → base_link` 連通 |
| **12/07** | ❌ 空的  | `view_frames`             | Empty TF Tree                 |

### 📊 分析結論

12/7 的 TF Tree 為空，**不是 TF 配置問題**，而是**上游 Driver 連線失敗**：

```
DTLS handshake failed
    ↓
Go2 Driver 無法連線
    ↓
/scan, /tf Topic 無資料
    ↓
SLAM 無法運作
    ↓
TF Tree 為空
```

**驗證方式：** 12/02 時 TF Tree 是正常的，代表 URDF 和 SLAM 配置都是對的。

### ✅ 今日回去後的驗證步驟

```bash
# 1. 重啟 Go2 機器狗

# 2. 環境檢查
zsh phase1_test.sh env

# 3. 啟動 Driver
zsh phase1_test.sh t1
# 等待看到 [go2_driver_node]: Connected

# 4. 驗證感測器
ros2 topic hz /scan  # 應該 ~5 Hz

# 5. 驗證 TF Tree 恢復
ros2 run tf2_tools view_frames
# 檢查 frames.pdf，應該看到:
# map → odom → base_link → front_camera / lidar_link

# 6. 即時監控 TF（推著狗走，看座標變化）
ros2 run tf2_ros tf2_echo map base_link
```

### 💡 學習心得

TF2 座標系統可以類比成 **前端 DOM Tree**：
- `map` = `<body>` (絕對座標)
- `odom` = `<div id="wrapper">` (相對位移)
- `base_link` = `<div id="robot">` (主容器)
- `front_camera` = `<img class="cam">` (子元素)

**`lookup_transform()` = `getBoundingClientRect()`** - 自動計算到根節點的累積轉換

---

## 📝 筆記區

```
=== Phase 0: 學習時間（機器狗不在身邊）===
- 14:50 - 16:00
- 閱讀專案必學知識清單
- 分析 TF 除錯歷史

=== Phase 1: 修復連線 ===
- 21:55 開始
- 遇到 DTLS handshake failed 問題
- 發現 WebRTC 連線不穩定（ping jitter 4ms ~ 184ms）
- 22:30 使用 zsh start_go2_simple.sh 成功連線
- 22:45 TF Tree 恢復正常，但 /scan 頻率偏低 (~0.8 Hz)

=== Phase 2: Nav2 驗證 ===
- 22:50 啟動 zsh phase1_test.sh t3 (SLAM + Nav2 + Foxglove)
- 23:00 Foxglove 成功連線 ws://192.168.1.200:8765
- 23:03 發送 /navigate_to_pose 目標
- 結果：機器狗有動，但因 /scan 頻率太低而卡住 (recovery: 7 次)

=== Phase 3: MCP 驗證（重大成功）===
- 23:21 測試 MCP publish_once /cmd_vel → ✅ 成功，機器狗移動！
- 23:25 啟動 snapshot_service，測試 call_service /capture_snapshot → ✅ 成功取得 JPEG！
- 23:27 完成 MCP 控制鏈驗證
```

---

## 🎉 今日成果總結

### ✅ 完成項目

| 項目 | 狀態 | 說明 |
|------|------|------|
| **Go2 Driver 連線** | ✅ | 使用 `zsh start_go2_simple.sh` 成功 |
| **TF Tree** | ✅ | `map → odom → base_link` 完整 |
| **Foxglove 視覺化** | ✅ | 可看到地圖、相機、TF |
| **MCP /cmd_vel 控制** | ✅ | 機器狗成功移動 |
| **MCP /capture_snapshot** | ✅ | 成功取得 JPEG 影像 |
| **Nav2 導航** | ⚠️ | 能動但卡住（/scan 頻率太低）|

### 🔗 MCP 控制鏈驗證成功

```
Kilo Code (LLM)
    ↓ MCP Protocol
ros-mcp-server
    ↓ WebSocket (Port 9090)
rosbridge_server
    ↓ ROS2
Go2 機器狗 ✅
```

---

## 📋 MCP 測試完整步驟

### 前置準備

```bash
# Terminal 1: 環境檢查
zsh phase1_test.sh env

# Terminal 2: rosbridge
zsh phase1_test.sh bridge

# Terminal 3: Driver + SLAM + Nav2 + Foxglove
zsh phase1_test.sh t3
# 或使用簡化版（只有 Driver）
# zsh start_go2_simple.sh

# Terminal 4: snapshot_service（重要！必須單獨啟動）
source ~/ros2_ws/install/setup.zsh
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
export CYCLONEDDS_URI=/home/roy422/local_only_v2.xml
ros2 run go2_robot_sdk snapshot_service
```

### 驗證服務已啟動

```bash
ros2 service list | grep snapshot
# 應該看到 /capture_snapshot
```

### Kilo Code MCP 測試指令

**測試 1：控制機器狗移動**
```
使用 publish_once 工具發布到 /cmd_vel：
- topic_name: /cmd_vel
- message_type: geometry_msgs/Twist
- message: {"linear": {"x": 0.2, "y": 0, "z": 0}, "angular": {"x": 0, "y": 0, "z": 0}}
```

**測試 2：擷取相機影像**
```
使用 call_service 呼叫 /capture_snapshot
- service_name: /capture_snapshot
- service_type: std_srvs/Trigger
```

**測試 3：停止機器狗**
```
使用 publish_once 工具發布到 /cmd_vel：
- topic_name: /cmd_vel
- message_type: geometry_msgs/Twist
- message: {"linear": {"x": 0, "y": 0, "z": 0}, "angular": {"x": 0, "y": 0, "z": 0}}
```

---

## ⚠️ 今日發現的問題

### 問題 1：/scan 頻率偏低

| Topic | 現在頻率 | 預期 |
|-------|---------|------|
| `/scan` | ~0.8-1.7 Hz | ~5 Hz |
| `/point_cloud2` | ~0.1-0.3 Hz | ~5-10 Hz |
| `/odom` | ~18 Hz | ~50 Hz |

**影響：** Nav2 costmap 更新太慢，規劃器失效

**可能原因：**
1. WebRTC 頻寬限制
2. Traffic Saving Mode 未完全關閉
3. WiFi 干擾

### 問題 2：Ping 延遲不穩定

```
之前：4ms ~ 184ms (mdev = 61ms) ❌
之後：6ms ~ 11ms (mdev = 1.4ms) ✅
```

**解決方案：** 靠近 Go2 / 減少 WiFi 干擾

---

## 🔗 相關資源

- [昨日進度 (12/7)](./2025-12-07-dev.md)
- [專題目標](../../00-overview/專題目標.md)
- [開發計畫](../../00-overview/開發計畫.md)
- [ros-mcp-server 文件](https://github.com/nicholasgubbins/ros-mcp-server)

---

## 🚀 W7 預備（12/9 開始）

根據開發計畫，W7 首要任務：

1. **視覺閉環整合**：LLM 描述「我看見前方有障礙物」
2. **優化 /scan 頻率**：解決 Nav2 卡住問題
3. **將 snapshot_service 整合到 robot.launch.py**

### W7 待辦

- [ ] 將 `snapshot_service` 自動加入 `robot.launch.py`
- [ ] 測試 VLM 圖片分析（Claude Vision）
- [ ] 優化 /scan 頻率（可能需要調整 WebRTC 設定）
- [ ] 重新驗證 Nav2 導航

---

## ✅ 今日成功標準（已完成）

- [x] Go2 Driver 連線成功（`/scan` 有數據）
- [x] TF Tree 正常
- [x] MCP /cmd_vel 控制成功
- [x] MCP /capture_snapshot 成功
- [ ] Nav2 導航成功（部分成功，需優化）
- [x] 筆記區記錄今日成果

---

**W6 MCP 基礎串接目標達成！✅ 🐕**
