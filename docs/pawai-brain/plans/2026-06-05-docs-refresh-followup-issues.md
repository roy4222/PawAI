# Docs 重構 follow-up issues 草稿（2026-06-05）

> 由 docs 架構重構 workflow 的 to-issues agent 萃取。這些是**不應在文件重構內完成**的後續工作（需 code / HITL / 上機）。尚未建立 GitHub issue——確認後可一鍵建。
> claim 真相源：[`docs/mission/2026-06-18-capability-claim-matrix.md`](../../mission/2026-06-18-capability-claim-matrix.md)。

## 1. voice.stop safety tie-break in intent_classifier + unit tests (FN=0 gate)
- **問題**：6/4 HITL trusted snapshot has voice.stop grade=fail (n=6, success=0.667, FN=2: R16 'no-ack' / R18 '欸等一下先停住' misclassified as come_here). Root cause is concrete and code-only: intent_classifier.classify() has no safety tie-break, so stop (weight 0.5) loses to come_here (1.0) when both keywords appear. Roadmap #6/#7 designed the fix but git confirms it is NOT implemented; speech code is unchanged since baseline so the fail is live. On stage, shouting '停' and the dog interpreting it as '過來' (a counter-command) is an audience-visible failure. This is the highest-leverage fix achievable BEFORE the 6/15 e2e rehearsal without hardware/HITL.
- **驗收**：(1) intent_classifier.classify() adds a safety tie-break: when stop primary keywords (停止/停下/停住) match with conf>=0.7, force stop to win over come_here; stop weight raised. (2) New unit tests for '欸等一下先停住' and '停住過來' both return stop and pass green. (3) No new overclaim introduced, no hardware touched. (4) After this code fix, a HITL retest per audit E-1 must hit FN=0 (quiet + noise, 10/10) BEFORE voice.stop may be wired to any motion or claimed as anything other than a convenience command (still scoreboard fail until retest passes).
- **需證據**：speech_processor new unit tests all green (including stop tie-break) + git diff; ros2-test-suite --quick shows no regression. Follow-up HITL evidence: new baseline JSONL/snapshot showing voice.stop FN=0 in quiet+noise (audit E-1 / H-voice.stop), recorded under docs/runbook/baseline-evidence/. Until that HITL run exists, voice.stop stays DO_NOT_CLAIM / brain_allowed=false.
- **Owner/lane**：speech (code by Codex in main repo, Claude review+git) + Go2/Jetson for the HITL retest　**優先級**：P0

## 2. gesture.wave HITL re-parametrize retest OR script palm/static fallback (no zero-reaction on stage)
- **問題**：6/4 HITL: gesture.wave grade=fail (n=9, recall=0.0, 6/6 positive all 'none', wave_pub=False throughout). The 6/5 professor meeting narrowed gestures to sit/raise-hand for recognition rate, so following the storyboard's '舉手' step would produce a literally-visible zero reaction on stage. Root cause is known: intermittent 1.5m hand detection + WaveDetector thresholds too strict (min_amplitude_px=50, vote_frames=10, stable_s=1.5). Note: the camera-wave fail is NOT the rtmpose config-default footgun — demo already overrides gesture_backend:=recognizer, and the running node logged recognizer with wave_pub=False (detector fed, ran, just never fired). This is genuine empirical/HITL+script work that the docs refresh cannot resolve.
- **驗收**：Either-or: (A) After HITL re-parametrize (gesture_min_score 0.1->0.05, min_amplitude_px 50->35, vote_frames/stable_s revert), gesture.wave passes the regression gate: wave event >=3/5 (off the 0/6 floor), some single standing position >=4/5, idle false-trigger 0/5, with idle ground-truth and n>=6 positive; chip flips to presentable. OR (B) demo-flow-plan S5 changes to palm/raise-hand static OR Studio-only event display, narration bound to '示意/刻意調慢防誤觸' (NOT '可靠'), and the fallback is confirmed to fire stably in full stack at the 6/17 rehearsal. MUST run via scripts/start_full_demo_tmux.sh (gesture_backend:=recognizer), NOT bare ros2 launch reading the rtmpose yaml. Gesture never wired to Go2 motion.
- **需證據**：(A) new baseline JSONL+snapshot showing wave retest result (pass, or still-fail but with n) under docs/runbook/baseline-evidence/; node log confirming wave_pub state. OR (B) demo-flow-plan S5 updated to palm/Studio fallback + 6/17 rehearsal recording confirming the fallback triggers. Confidence value must not be hardcoded 1.0; if it is, mark 'no discriminative power' and scoreboard must not pass on it alone.
- **Owner/lane**：Go2/Jetson HITL + brain-studio-lane / vision (co-schedule with face #81 re-run to amortize stage time)　**優先級**：P0

## 3. Run full ①-⑥ coherent demo script e2e before 6/15 + power/long-run monitoring rehearsal
- **問題**：The 6/5 professor meeting set a hard deadline AND self-admitted the full ①-⑥ script has NEVER been run end-to-end. Each capability has a 6/4 standalone baseline, but post-integration resource contention, long-run degradation, and XL4015 power cut-outs (Go2 has cut power 8+ times during operation — the single largest demo failure point) have never been verified together. This is the only way to turn 'a pile of individually-passing capabilities' into 'a demo that can actually be performed start to finish'. It is hardware/HITL work, explicitly out of scope for the docs refresh.
- **驗收**：Under start_full_demo_tmux.sh, run ①get-ready ②recognize+greet ③sit response ④cup recognition ⑤raise-hand/palm interaction ⑥safety-layer refusal continuously >=1 time with Studio synchronously displaying trace. During the run, record supply voltage / temperature / RAM headroom; after 30 minutes re-test each capability for degradation; for each strongly-backed segment (face/object/safety) have a pre-recorded video fallback ready; produce a 'segments that must switch to pre-recorded' list. Every demo start must first run the E-5 healthcheck (sed CRLF strip on .env/.env.local; after pawai demo start, tmux ls + ros2 node list + ros2 topic list — do not trust the CLI '✓ Demo running' which can be a silent false-success).
- **需證據**：e2e run recording + per-segment Studio trace screenshots; supply-voltage/temperature/RAM log; 30-minute long-run re-test results; the 'must switch to pre-recorded' list as a demo-preflight report.
- **Owner/lane**：Roy (demo-preflight + all-lane integration) + Go2/Jetson HITL + a dedicated power-monitoring person　**優先級**：P0

## 4. face.recognition clean re-run: >=2 enrollees, multi-lighting, REAL strangers, near-approach dynamics
- **問題**：6/4 face is trusted pass (n=9, recall=1.0, false-accept=0.0, p50≈74ms) but only narrow-version: a single enrollee (roy), single lighting, lowest positive conf just 0.2378 (near threshold), and idle=empty scene (zero evidence value for real stranger rejection — README explicitly caveats real-stranger rejection unverified). With only 1 enrollee, wrong_person_count pass-threshold=0 is effectively untested. The demo shot is 'walk in -> recognize roy -> welcome back', which requires verifying near-approach dynamics + at least one stranger not being falsely greeted. This is HITL evidence-collection the docs refresh cannot produce; it underpins whether the face claim can move off CLAIM_WITH_CAVEAT.
- **驗收**：Per audit H-face: (1) main-camera registered recall 5/5 names stable and non-jittery; (2) near-approach 2.5m->1.6m >=4/5 locked once steady (unknown allowed while walking); (3) REAL stranger (not empty frame) 0/3 false-accept (any stranger greeted 'welcome back' = no-go, fall back to 'someone is approaching, greeting' without naming); (4) side-face 30-45° + half-backlight degraded tolerance recall>=0.60. Optional multi-image re-enroll A/B to test the 'low conf = enroll-quality' hypothesis. Hard pass gate: recall>=80%, false-accept<=3%, wrong_person>=1 must NOT pass.
- **需證據**：New fixed-window baseline JSONL+snapshot under docs/runbook/baseline-evidence/ with >=2 enrollees, multi-lighting, real stranger idle, near-approach; conf distribution moved off the 0.24 boundary; full-stack long-run re-test result. Tied to baseline issue #81.
- **Owner/lane**：Go2/Jetson HITL (face lane)　**優先級**：P1

## 5. object.cup distance baseline at 1m / 1.5m / 2m + D435 depth-measured distance + idle-with-clutter
- **問題**：6/4 object.cup is pass but narrow-version only: 5/5 @1m, conf 0.83–0.88, single-color cup, tabletop, fixed light, distance=manual_declared (not measured), latency p90≈4.9s (first frame). No 2m samples, no ground-level cup, idle only empty-table (never verified non-false-report with people/objects present). Recall drops with distance but is unquantified. The demo placement distance (~1-2m) and audience viewing angle are untested. This is HITL distance-curve work that pins the narrow claim; docs refresh cannot produce the numbers.
- **驗收**：Per audit E-2 / H-object.cup, using capture_baseline_round.py (fixed-window, SSH-driven, --gesture-topic /__no_gesture__ to isolate pollution) AFTER TRT warmup (ros2 topic hz /perception/object/debug_image != 0; discard cold-start window): tabletop cup 1m >=5 rounds + 2m >=5 rounds (scenario_kind=positive) + empty-scene 60s × >=3 windows (idle) + idle-with-clutter (book/phone/hand 1-2m). Confirm yaml: model=yolo26n.onnx, input=640, conf=0.35, whitelist=[41,999]. Pass gates: 1m recall>=80% pass / 60-80% degraded / <60% fail; idle single-window 0=pass / 1=degraded / >1=fail (hard gate); latency observe-only. Fallback: 2m degraded -> claim near-range (~1m) only, 2m via Studio bbox evidence; try conf 0.35->0.30 + light/ROI before any YOLO26s spike (which must co-measure full-stack FPS+RAM headroom>=0.8GB).
- **需證據**：build_scoreboard --preflight (built at a SHA matching the Jetson deploy manifest) + pawai readiness; new baseline JSONL+snapshot showing per-distance recall and idle false-positive under docs/runbook/baseline-evidence/.
- **Owner/lane**：Go2/Jetson HITL (object lane)　**優先級**：P1

## 6. nav F7 fresh-stack root cause: goal accepted but /cmd_vel_nav has no publisher
- **問題**：6/4 nav.* all insufficient_data (n=0). The live dry-run aborted at the AMCL gate (amcl_lost), actual_distance=0, Go2 zero motion — proving only that the action chain is wired and fail-closed is correct. F7 (after goal accept, /cmd_vel_nav has no publisher → 10s timeout) was scheduled for verification 5/13 but no result was filed, and it has NEVER been root-caused in a fresh stack. Per north-star §7 iron rule, no real motion until safe_stop/no_auto_resume pass (or human override) with power stable + e-stop. This is hardware/HITL diagnosis the docs refresh cannot do; it gates whether any real nav motion is possible for 6/18.
- **驗收**：Per roadmap #4 + audit H-nav: cleanly restart full stack -> set initialpose -> send 0.3m goto_relative -> capture controller_server log / costmap / lifecycle state to identify why /cmd_vel_nav has no publisher; record root cause in roadmap. Only after F7 no longer reproduces AND power is stable AND e-stop is in hand AND Roy approves may a single human-override 0.3m motion be attempted; safe_stop must hit 5/5 stop-before-collision (zero tolerance) before any obstacle-stop claim. NO dynamic avoidance, NO 1m+, NO continuous multi-goal. Default demo form stays 'Foxglove edge-perception display only, Go2 zero self-drive'.
- **需證據**：Root-cause write-up in roadmap with controller_server log / lifecycle evidence; if real motion is attempted, motion_sign_off record + safe_stop 5/5 evidence under docs/runbook/baseline-evidence/. Tied to baseline issue #84 (nav). Reminder: /event/nav/mission topic does NOT exist in source — observe the /nav/goto_relative action Result (success/message/actual_distance), never echo the phantom topic (it hangs forever).
- **Owner/lane**：nav-avoidance-lane + Go2/Jetson HITL　**優先級**：P1

## 7. Brain grounding verifier + anti-hallucination cleanup (remove rain/cup/pose few-shot, gate _get_weather)
- **問題**：Brain anti-hallucination is grade=fail: the 6/4 operator observed the persona self-fabricating '外面在下雨' / 'I saw a cup' / posture statements that have no sensor backing. This directly punctures the 'honest scoreboard' which is the ~40% engineering-credibility core selling point of 6/18. Current code actively manufactures it: STYLE.md L64, EXAMPLES.md L21/30/65 few-shots, and world_state_builder.py _get_weather() injecting wttr.in Taipei network weather; validator/repair has zero anti-hallucination grep. There is ZERO measurement of brain.skill_gate / brain.trace (n=0, brain_allowed=false). This is code + measurement work out of scope for the docs refresh, and anti-hallucination stays DO_NOT_CLAIM until done.
- **驗收**：(1) Implement a grounding verifier (response_repair WARN per roadmap #5) that flags claims with no world_state backing; (2) delete the un-grounded rain/cup/pose few-shots from EXAMPLES.md (L21/30/65) and add counter-examples; add IDENTITY/STYLE anti-hallucination hard constraint ('pose=None means not seen, must not fabricate' per #19); (3) gate/disable world_state_builder _get_weather() network injection so persona cannot present network weather as real perception; (4) separately, brain.safety_refusal e2e (the demo ⑥ main show) must be HITL-verified: standard '請翻跟斗' 10/10 refused + Studio red badge (banned_api:1301) synced + Go2 zero motion, via REAL studio_gateway (not mock), with 91-test pytest screenshot backup, and safety_gate stop-keyword short-circuit 5/5.
- **需證據**：Grounding verifier code + unit tests + git diff; EXAMPLES.md/STYLE.md/IDENTITY diff removing hallucination few-shots; _get_weather gating diff. For safety-layer: HITL run showing 10/10 refusal + badge sync against real gateway, recorded under docs/runbook/baseline-evidence/. Until anti-hallucination is implemented+verified it stays fail/DO_NOT_CLAIM; until safety_refusal is HITL-run it stays insufficient_data (mechanism-exists+unit-test only, CLAIM_WITH_CAVEAT max).
- **Owner/lane**：brain-studio-lane (code by Codex, Claude review) + Go2/Jetson HITL for safety_refusal e2e　**優先級**：P1

## 8. Studio evidence trace e2e verification during the coherent demo run
- **問題**：studio.evidence is 6/4 insufficient_data (n=0, not measured this round). The demo's honesty narrative depends on Studio showing each capability chip + trace + debug_image synchronously to prove the demo is live perception, not hardcoded. But Studio displaying a result must NOT be treated as verification of the capability itself, and there is no measurement of trace synchronicity or evidence completeness. This can only be measured during the actual e2e demo run (depends on the 6/15 e2e issue), so it is out of scope for the docs refresh.
- **驗收**：During the full ①-⑥ e2e run, measure Studio trace synchronicity (chip/trace/debug_image arrive in step with the underlying events) and evidence completeness across all demo segments. Studio is presented as an evidence carrier showing live perception is not hardcoded — it is NOT claimed as 'studio.evidence pass' on its own. Verify against real studio_gateway (not mock), consistent with the brain safety-badge path (chat-panel.tsx:543 / badge banned_api:1301).
- **需證據**：Per-segment Studio trace screenshots time-aligned with the e2e run + a note on any desync; recorded as part of the demo-preflight report. studio.evidence remains NEEDS_RETEST (display-only carrier) until this e2e measurement exists — do not mark pass.
- **Owner/lane**：brain-studio-lane + Go2/Jetson HITL (runs with the 6/15 e2e issue)　**優先級**：P2

## 9. pose baseline observer + lock pose.fall to non-triggering (unblock pose measurement)
- **問題**：pose.basic/pose.fall are 6/4 insufficient_data (n=0, no pose observer, fall claim_level=future, brain_allowed=false). The demo's ③ 'sit -> respond' segment requires sitting detection, but the capture tool has no pose mode, so it is entirely unmeasured. Separately, pose.fall is designed 'demo-only but can still trigger skill', which conflicts with North Star §4/§5 and risks interrupting dialogue + overclaim. This is observer-tooling code + HITL ground-truth collection, out of scope for the docs refresh; until done, pose stays DO_NOT_CLAIM and the demo uses an application-scenario video instead of a live fall.
- **驗收**：(1) Add normalize_pose_event() to perception_baseline_observer.py (roadmap #17) subscribing /event/pose_detected, wired into the capture round so pose.basic becomes measurable. (2) Lock pose.fall so the fallen branch only records world_state and does NOT emit a skill (feature-flag / brain_node _on_pose guard, roadmap #18); snapshot stays insufficient_data; demo keeps enable_fallen:=false. (3) HITL per H-pose.sitting: sit-after-stand triggers sitting within 1.5s >=4/5, bend/crouch 0 false-positive as sitting; if unmet, ③ degrades to 'Studio pose panel shows sitting, no voice response' and narration never mentions fall even when the Studio fallen-red badge appears (fallen hallucination is known-present).
- **需證據**：pose observer + normalize_pose_event code + unit tests + git diff; pose.fall non-trigger guard diff; if HITL run done, new pose baseline JSONL+snapshot under docs/runbook/baseline-evidence/. fall stays future / not in demo.
- **Owner/lane**：Go2/Jetson HITL (pose lane) + vision (observer code)　**優先級**：P2

## 10. voice.command core-phrase + noise + speaker-variant retest (move off single-condition narrow pass)
- **問題**：6/4 voice.command is pass (n=24, success_rate=0.875, false-trigger=0.0) but narrow: latency all null, CSV rebuilt from terminal, git_commit != snapshot SHA, single speaker, single mic, single phrasing. Real failures exist (greet->empty, take_photo->chat, status->chat) from colloquial boundary words. The demo only uses a few fixed core lines, but those need same-phrase repeat-stability + colloquial-variant boundary mapping + on-site noise verification. This is HITL evidence the docs refresh cannot produce; it keeps the claim at CLAIM_WITH_CAVEAT until done.
- **驗收**：Per audit H-voice.command: (1) demo core fixed phrases (greet/take_photo/status, 1 line each) at ~0.5m quiet, success_rate>=0.80, demo core lines >=9/10 (5 each, 15 total); (2) same-intent colloquial variants degraded>=0.70 or lock to fixed phrasing (no improvisation); (3) on-site noise success_rate>=0.80, <0.70 -> close-mic + quiet-the-room. Measure full ASR->intent e2e on the demo mic >=20 entries including take_photo/status; record e2e latency; switch to a non-roy speaker + noise. Do not call 0.875 an 'ASR recognition rate'.
- **需證據**：New baseline JSONL+snapshot with measured e2e latency, noise condition, and a second speaker under docs/runbook/baseline-evidence/; ros2 topic echo /event/speech_intent_recognized capture for the core lines.
- **Owner/lane**：Go2/Jetson HITL (speech lane)　**優先級**：P2

