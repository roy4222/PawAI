# 今日開發計畫 - 2025/12/07 (六)

**目標：** W6 收尾 + W7 預備（視覺閉環基礎）

---

## 📊 專案進度概覽

| 週次 | 目標 | 狀態 |
|------|------|------|
| **W6** (12/2-12/8) | MCP 基礎串接 | ✅ 12/6 提前完成！ |
| **W7** (12/9-12/15) | 視覺閉環 + Action 驗證 | ⏳ 待開始 |
| **W8** (12/16-12/22) | 尋物情境整合 | ⏳ |
| **W9-W10** (12/23-1/6) | Demo 準備 | ⏳ |

---

## 🎯 今日任務

### Phase 1：W6 補完（可選）

| 任務 | 優先級 | 說明 |
|------|--------|------|
| Windows wscat 測試 | 🟢 低 | 從 Windows 測試 `ws://192.168.1.200:9090` |
| 文件更新 | 🟢 低 | 更新開發計畫.md 標記 W6 完成 |

### Phase 2：W7 預備工作（推薦）

| 任務 | 優先級 | 說明 |
|------|--------|------|
| **snapshot_service 骨架** | 🔴 高 | 建立 `/capture_snapshot` 服務框架 |
| **Action 驗證準備** | 🔴 高 | 研究 `send_action_goal()` 用法 |
| **SLAM + Nav2 測試** | 🟡 中 | 確認 Nav2 能正常導航 |

---

## 📋 Phase 2.1：snapshot_service 開發

### 目標
讓 LLM 能透過 MCP 擷取相機影像，回傳 base64 JPEG。

### 規格
```python
# 預期 API
# MCP 呼叫: call_service('/capture_snapshot', 'std_srvs/Trigger')
# 回傳: {'success': True, 'message': '<base64 encoded JPEG>'}
```

### 檔案位置
```
src/go2_robot_sdk/go2_robot_sdk/snapshot_service.py
```

### 基本架構
```python
#!/usr/bin/env python3
import rclpy
from rclpy.node import Node
from sensor_msgs.msg import Image
from std_srvs.srv import Trigger
from cv_bridge import CvBridge
import cv2
import base64

class SnapshotService(Node):
    def __init__(self):
        super().__init__('snapshot_service')
        self.bridge = CvBridge()
        self.latest_image = None
        
        # 訂閱相機
        self.create_subscription(
            Image, '/camera/image_raw', 
            self.image_callback, 10
        )
        
        # 建立服務
        self.create_service(
            Trigger, '/capture_snapshot', 
            self.capture_callback
        )
        
        self.get_logger().info('Snapshot service ready')
    
    def image_callback(self, msg):
        self.latest_image = msg
    
    def capture_callback(self, request, response):
        if self.latest_image is None:
            response.success = False
            response.message = 'No image available'
            return response
        
        try:
            # 轉換為 OpenCV 格式
            cv_image = self.bridge.imgmsg_to_cv2(
                self.latest_image, 'bgr8'
            )
            
            # 壓縮為 JPEG (640x480)
            resized = cv2.resize(cv_image, (640, 480))
            _, jpeg = cv2.imencode(
                '.jpg', resized, 
                [cv2.IMWRITE_JPEG_QUALITY, 60]
            )
            
            # Base64 編碼
            b64 = base64.b64encode(jpeg.tobytes()).decode()
            
            response.success = True
            response.message = b64
        except Exception as e:
            response.success = False
            response.message = str(e)
        
        return response

def main():
    rclpy.init()
    node = SnapshotService()
    rclpy.spin(node)
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

---

## 📋 Phase 2.2：MCP Action 驗證準備

### 目標
確認 `send_action_goal()` 能否控制 Nav2 導航。

### 前置需求
1. SLAM + Nav2 正常運行
2. 已有地圖（`maps/phase1.yaml`）

### 測試步驟

**Step 1：啟動完整導航堆疊**
```bash
# Terminal 1: 環境 + rosbridge
zsh phase1_test.sh env
zsh phase1_test.sh bridge

# Terminal 2: Go2 Driver + SLAM + Nav2
ros2 launch go2_robot_sdk robot.launch.py slam:=true nav2:=true
```

**Step 2：在 Kilo Code 測試 Action**
```
請使用 send_action_goal 工具：
- action_name: /navigate_to_pose
- action_type: nav2_msgs/action/NavigateToPose
- goal: {
    "pose": {
      "header": {"frame_id": "map"},
      "pose": {
        "position": {"x": 1.0, "y": 0.0, "z": 0.0},
        "orientation": {"w": 1.0}
      }
    }
  }
- timeout: 30.0
```

### 預期結果
- ✅ 成功 → 省下 FSM Adapter 開發
- ❌ 失敗 → 開發 FSM Adapter 作為備案

---

## 📋 Nav2 手動測試完整指南

### 🚀 Step 0：環境準備

```bash
# 開 Terminal，執行環境檢查
cd ~/ros2_ws/src/elder_and_dog
zsh phase1_test.sh env
```

---

### 🚀 Step 1：啟動 rosbridge（Terminal 1）

```bash
cd ~/ros2_ws/src/elder_and_dog
zsh phase1_test.sh bridge
# 應該看到：Rosbridge WebSocket server started on port 9090
```

---

### 🚀 Step 2：啟動 SLAM + Nav2 + Foxglove（Terminal 2）

```bash
cd ~/ros2_ws/src/elder_and_dog
zsh phase1_test.sh t3
# 等待看到：
#   - [slam_toolbox] Ready to process incoming data
#   - [bt_navigator] Creating
#   - Foxglove: Server listening on port 8765
```

---

### 🚀 Step 3：啟動 snapshot_service（Terminal 3，可選）

```bash
cd ~/ros2_ws/src/elder_and_dog
source install/setup.zsh
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
export CYCLONEDDS_URI=/home/roy422/local_only_v2.xml
ros2 run go2_robot_sdk snapshot_service
```

---

### 🚀 Step 4：連線 Foxglove 驗證

1. 開啟 Foxglove Studio
2. 連線至 `ws://192.168.1.200:8765`
3. 應該看到：
   - 3D 面板有地圖顯示
   - 右側有相機影像

---

### 🚀 Step 5：手動發送導航目標（CLI 測試）

開一個新 Terminal (Terminal 4)：

```bash
cd ~/ros2_ws/src/elder_and_dog
source install/setup.zsh
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
export CYCLONEDDS_URI=/home/roy422/local_only_v2.xml

# 測試 1：發送導航目標 (前方 1 公尺)
ros2 action send_goal /navigate_to_pose nav2_msgs/action/NavigateToPose \
  "{pose: {header: {frame_id: 'map'}, pose: {position: {x: 1.0, y: 0.0, z: 0.0}, orientation: {w: 1.0}}}}" \
  --feedback

# 測試 2：發送導航目標 (右前方 1.5 公尺)
ros2 action send_goal /navigate_to_pose nav2_msgs/action/NavigateToPose \
  "{pose: {header: {frame_id: 'map'}, pose: {position: {x: 1.0, y: -0.5, z: 0.0}, orientation: {w: 1.0}}}}" \
  --feedback
```

---

### 🚀 Step 6：透過 MCP 發送導航目標

確認手動 CLI 成功後，在 Kilo Code (ROS2專家 Mode) 中：

```
發送導航目標到座標 (x=1.0, y=0.0)：
- action_name: /navigate_to_pose
- action_type: nav2_msgs/action/NavigateToPose
- goal: {"pose": {"header": {"frame_id": "map"}, "pose": {"position": {"x": 1.0, "y": 0.0, "z": 0.0}, "orientation": {"w": 1.0}}}}
- timeout: 60.0
```

---

### ⚠️ 疑難排解

| 問題 | 可能原因 | 解決方案 |
|------|----------|----------|
| 機器狗原地打轉 | 目標點太近或在障礙區 | 換更遠的目標點 (x > 1.0) |
| Action timeout | Nav2 規劃失敗 | 檢查 costmap，或設定更長 timeout |
| 收不到 /scan | Driver 未啟動 | 確認 `t3` 有啟動 Go2 Driver |
| TF 錯誤 | SLAM 未收斂 | 等待幾秒讓 SLAM 穩定 |

---

## 📋 快速指令摘要

| Terminal | 指令 | 用途 |
|----------|------|------|
| T1 | `zsh phase1_test.sh bridge` | rosbridge (Port 9090) |
| T2 | `zsh phase1_test.sh t3` | SLAM + Nav2 + Foxglove |
| T3 | `ros2 run go2_robot_sdk snapshot_service` | 影像擷取服務 |
| T4 | `ros2 action send_goal ...` | 手動發送導航目標 |

---

## ⚠️ 注意事項（重要！）

### 1️⃣ snapshot_service 註冊步驟

建檔後**必須**完成以下步驟才能使用：

```bash
# 1. 編輯 setup.py，在 entry_points 加入：
#    'snapshot_service = go2_robot_sdk.snapshot_service:main'

# 2. 重建 package
cd ~/ros2_ws
colcon build --packages-select go2_robot_sdk

# 3. 重新載入環境
source install/setup.zsh

# 4. 測試執行
ros2 run go2_robot_sdk snapshot_service
```

若要用 launch 啟動，需在 `robot.launch.py` 增加該 node。

### 2️⃣ Nav2 / Action 驗證注意

> ⚠️ **使用既有地圖時，必須先設定 Initial Pose！**

- AMCL 需要初始位置才能收斂
- 未設定 Initial Pose 直接送 `/navigate_to_pose` → **Action 會卡住**
- 使用 SLAM 即時建圖時，預設 (0,0) 較少問題

**設定 Initial Pose 方法：**
1. 在 Foxglove / RViz2 用「2D Pose Estimate」工具點擊
2. 或透過 `/initialpose` topic 發布

### 3️⃣ rosbridge 單一實例

> ⚠️ **啟動前務必檢查 Port 9090！**

```bash
# 檢查是否已有 rosbridge 運行
ss -tlnp | grep 9090

# 若有輸出，記下 PID 並決定是否沿用
# 若需重啟，先 kill 舊的
sudo kill <PID>
```

### 4️⃣ 控制安全

- **測試 `/cmd_vel` 時保持低速**（≤ 0.3 m/s）
- 若 **Action 驗證失敗**，立即轉 **Plan B**：
  1. 開發 FSM / Safety Layer
  2. 限制最大速度與加速度
  3. 實作急停功能
  4. 再轉接 Nav2

### 5️⃣ 進度同步

完成後記得：
1. 在本文件勾選「今日成功標準」
2. 更新 `開發計畫.md` 標註 W6 完成

---

## 📊 今日成功標準

- [x] snapshot_service 骨架完成（含 setup.py 註冊）✅
- [x] QoS 問題修復（BEST_EFFORT 匹配 Go2 Driver）✅
- [x] MCP 呼叫 /capture_snapshot 成功 ✅
- [x] Nav2 導航堆疊正常運行 ✅
- [x] Action 驗證測試 ✅ （MCP 成功發送，但 Nav2 控制器需調整）
- [ ] 更新開發計畫.md 標記 W6 完成

---

## �️ 今日詳細執行計畫

### ⏰ 時間安排（建議）

| 時段 | 任務 | 預估時間 |
|------|------|----------|
| **Phase 1** | snapshot_service 開發 | ~30 分鐘 |
| **Phase 2** | MCP Action 驗證 | ~45 分鐘 |
| **Phase 3** | 文件更新 & 收尾 | ~15 分鐘 |

---

### 🔧 Phase 1：snapshot_service 開發 ✅ 完成！

**目標：** 讓 LLM 能透過 MCP 擷取相機影像

#### ✅ 完成項目

| 步驟 | 狀態 | 說明 |
|------|------|------|
| 建立 `snapshot_service.py` | ✅ | 支援參數化 (topic/quality/size) |
| 修改 `setup.py` | ✅ | 註冊 entry_point |
| colcon build | ✅ | 0.91s 編譯成功 |
| QoS 修復 | ✅ | BEST_EFFORT 匹配 Go2 Driver |
| ROS2 CLI 測試 | ✅ | `ros2 service call /capture_snapshot` |
| MCP 整合測試 | ✅ | Kilo Code 成功呼叫並回傳 base64 JPEG |

#### 關鍵修復：QoS 不相容

原始問題：
```
[WARN] New publisher discovered on topic '/camera/image_raw', offering incompatible QoS. 
Last incompatible policy: RELIABILITY
```

解決方案：使用 `BEST_EFFORT` QoS 匹配 Go2 Driver：
```python
best_effort_qos = QoSProfile(
    reliability=QoSReliabilityPolicy.BEST_EFFORT,
    history=QoSHistoryPolicy.KEEP_LAST,
    depth=1
)
```

---

### 🎯 Phase 2：MCP Action 驗證（🔴 高優先）

**目標：** 確認 `send_action_goal()` 能否控制 Nav2

#### Step 2.1：啟動完整導航堆疊
```bash
# Terminal 1: 環境 + rosbridge
zsh phase1_test.sh env
zsh phase1_test.sh bridge

# Terminal 2: Go2 + SLAM + Nav2
ros2 launch go2_robot_sdk robot.launch.py slam:=true nav2:=true
```

#### Step 2.2：設定 Initial Pose（重要！）
> ⚠️ **必須在 Foxglove / RViz2 設定初始位置，否則 Action 會卡住！**

#### Step 2.3：在 Kilo Code 測試 Action
```
請使用 send_action_goal 工具：
- action_name: /navigate_to_pose
- action_type: nav2_msgs/action/NavigateToPose
- goal: {"pose": {"header": {"frame_id": "map"}, "pose": {"position": {"x": 1.0, "y": 0.0}, "orientation": {"w": 1.0}}}}
- timeout: 30.0
```

#### Step 2.4：記錄結果
- ✅ 成功 → 直接用，省下 FSM Adapter 開發
- ❌ 失敗 → 開發 FSM Adapter 作為備案 (W7 任務)

---

### 📄 Phase 3：文件同步

- [ ] 更新 `開發計畫.md` 標記 W6 完成
- [ ] 更新本文件「今日成功標準」
- [ ] 整理筆記區

---

## 📝 筆記區

```
=== Phase 1: snapshot_service ===
- 07:20 開始今日開發
- 07:31 Phase 1 開始
- 07:35 cv_bridge ✅, OpenCV 4.11.0 ✅
- 07:38 snapshot_service.py 建立完成
- 07:40 colcon build 成功 (0.98s)
- 07:54 發現 QoS RELIABILITY 不相容問題
- 07:56 修復 QoS → BEST_EFFORT
- 07:58 /capture_snapshot 服務上線
- 07:59 ROS2 CLI 測試成功（收到 base64 JPEG）
- 08:01 🎉 Kilo Code MCP 呼叫成功！

=== Phase 2: MCP Action 驗證 ===
- 11:10 重新啟動 SLAM + Nav2 + Foxglove
- 11:15 驗證 /navigate_to_pose action 可用
- 11:18 第一次測試: 機器狗原地打轉
- 11:19 分析: 初始位置不在原點，目標點在後方
- 11:20 調整目標到相對位置，仍然打轉
- 11:21 確認: MCP → rosbridge → Nav2 通信成功
- 11:22 問題定位: Nav2 controller 參數不適合 Go2

📌 Phase 1 里程碑達成：LLM 可透過 MCP 擷取 Go2 相機影像
📌 Phase 2 里程碑達成：MCP send_action_goal 可與 Nav2 通信（但實際導航需調整參數）
```

---

## ⚠️ 今日發現的問題

### 問題 1：QoS 不相容 (Phase 1)

**現象：**
```
[WARN] New publisher discovered on topic '/camera/image_raw', offering incompatible QoS. 
Last incompatible policy: RELIABILITY
```

**原因：** Go2 Driver 使用 `BEST_EFFORT` QoS，但 snapshot_service 預設使用 `RELIABLE`

**解決方案：** 修改 subscription 使用 `BEST_EFFORT`：
```python
best_effort_qos = QoSProfile(
    reliability=QoSReliabilityPolicy.BEST_EFFORT,
    history=QoSHistoryPolicy.KEEP_LAST,
    depth=1
)
```

### 問題 2：Nav2 機器狗原地打轉 (Phase 2)

**現象：**
- 發送導航目標後，機器狗只在原地打轉
- `distance_remaining` 始終維持 ~0.9 公尺
- `number_of_recoveries: 0`（表示沒有觸發 recovery）
- orientation.z 一直變化（-0.17 → -0.11 → -0.13）

**測試資料：**
```
目標點: (x=1.36, y=-0.60)
當前位置: (x=0.43, y=-0.14)
distance_remaining: 0.9553884863853455
orientation.z: -0.17 → -0.14 → -0.11 (只轉不走)
```

**可能原因：**
1. Nav2 controller 參數不適合 Go2 四足機器人
2. `/cmd_vel` 速度指令可能沒有正確傳遞
3. costmap 膨脹參數可能過大

**建議解決方案：**
1. 檢查 `nav2_params.yaml` 中的 controller 設定
2. 暫時使用 `/cmd_vel` 直接控制作為 Demo 備案
3. W7/W8 調整 Nav2 參數

---

## ✅ 今日成果總結

| 項目 | 結果 | 說明 |
|------|------|------|
| **snapshot_service** | ✅ 成功 | 可透過 MCP 擷取 base64 JPEG |
| **MCP Action 發送** | ✅ 成功 | `send_action_goal` 可與 Nav2 通信 |
| **Nav2 實際導航** | ⚠️ 需調整 | 控制器參數需針對 Go2 優化 |

### 🔗 MCP 控制鏈驗證

```
Kilo Code (LLM) 
    ↓ MCP Protocol
ros-mcp-server 
    ↓ WebSocket (Port 9090)
rosbridge_server 
    ↓ ROS2
Nav2 bt_navigator 
    ↓ Action
/navigate_to_pose ✅
```

### 🚀 下一步 (W7)

1. 調整 Nav2 controller 參數
2. 或改用 `/cmd_vel` 直接控制 + FSM Adapter
3. 整合 snapshot_service 到 robot.launch.py

---

## 🔗 相關資源

- [昨日進度 (12/6)](./2025-12-06-dev.md)
- [開發計畫](../../00-overview/開發計畫.md)
- [ros-mcp-server 文件](https://github.com/nicholasgubbins/ros-mcp-server)

