# 2025-12-23 開發筆記 (W9 Day 1)

**日期：** 2025/12/23（星期一）  
**目標：** 系統問題調查 + 優化計畫制定  
**關鍵里程碑：** 2026/1/7 第一階段發表（剩餘 15 天）

---

## 📊 專案狀態總覽

| 指標 | 數值 |
|------|------|
| **當前週次** | W9 Day 1 |
| **整體進度** | 約 95% |
| **發表倒數** | 15 天 |

---

## 🔍 今日調查：四大問題確認

### 問題 1：Go2 發出聲音

**結論：** ✅ **已完整實作，無需改進**

| 項目 | 說明 |
|------|------|
| **檔案** | `speech_processor/speech_processor/tts_node.py` (475 行) |
| **技術** | ElevenLabs API + WebRTC 音訊通道 (API ID: 4001-4005) |
| **整合** | 已納入 `robot.launch.py` (line 222) |
| **使用** | `ros2 topic pub /tts std_msgs/msg/String "data: '你好'"` |

> ⚠️ **注意：** 需設定 `ELEVENLABS_API_KEY` 環境變數

---

### 問題 2：語意尋物 vs 自動避障 🔴

**結論：** 兩條流程未整合，遮擋問題確實存在

**現況架構：**
```
find_object MCP Tool
    ↓
拍一張照片 → GPU 分析 → 回傳結果
    ↓
[流程結束] ❌ 無後續動作
```

**缺失的邏輯：**
- ❌ 物品不在視野內時，無下一步決策
- ❌ 遇到障礙物時，不會繞過再搜尋
- ❌ 搜尋失敗後無備案策略

**文件佐證：**
- `開發計畫.md` (line 19)：Demo 導航＝視覺導航、暫不用 Nav2
- `專題目標.md` (line 262)：避障是由 LLM 看到障礙物再轉向
- `2025-12-21-dev.md` (line 114)：YOLO+DA3 回傳方向/距離與 cmd_vel

---

### 問題 3：Depth Anything V2 + YOLO World 座標轉換

**結論：** ✅ 已完成（影像座標對齊，非 3D 轉換）

**當前實作（在 GPU Server 上）：**

```python
# YOLO World 輸出
bbox = [x, y, width, height]  # 像素座標
center = [x + w/2, y + h/2]   # bbox 中心

# Depth Anything V2 輸出
depth_map = model.infer_image(image)  # 每個像素的深度（公尺）
distance = depth_map[center[1], center[0]]  # 取 bbox 中心的深度

# 距離校正（分段線性）
if distance < 0.8:
    calibrated = distance * 0.66  # SCALE_NEAR
elif distance < 3.0:
    calibrated = distance * 0.23  # SCALE_MID  
else:
    calibrated = distance * 0.20  # SCALE_FAR

# 方位計算（簡易三分法，像素閾值 213/427）
if center[0] < 213:
    direction = "左側"
    angular_z = 0.3
elif center[0] > 427:
    direction = "右側"
    angular_z = -0.3
else:
    direction = "正前方"
    angular_z = 0.0
```

**關鍵特點：**
- ❌ 未使用相機內參矩陣 K
- ❌ 未進行 TF 轉換到 map 座標系
- ✅ 分段線性校正（SCALE_NEAR/MID/FAR）
- ⚠️ 校正參數只針對椅子優化

---

### 問題 4：只有 chair 準確 🔴

**結論：** 校正參數是針對椅子手動調整的

**證據：**
| 物件 | 校正後誤差 | 狀態 |
|------|-----------|------|
| 椅子 | 4.4% | ✅ |
| 包包 | 65% | ❌ |

**根本原因分析：**

1. **材質反射特性不同**
   - 椅子：木質/塑膠（反射穩定）
   - 包包：布料/皮革（反射不規則）

2. **物體大小差異**
   - 椅子：大面積平面（深度採樣點多）
   - 包包：小物件（深度採樣點少，噪音大）

3. **單一校正參數局限**
   - 當前：所有物件用同一組 SCALE 參數
   - 應該：不同物件使用不同校正參數

---

## 🎯 優化計畫

### 方案 A：主動探索模式 🔴 必做

**核心思想：** 找不到物品 → 移動 → 再找 → 重複（最多 N 次）

**實作架構：**

```python
# 新增檔案：ros-mcp-server/active_search.py

class ActiveSearchManager:
    """主動探索管理器"""

    async def search_with_exploration(
        self,
        target: str,
        max_attempts: int = 5,
        strategy: str = "rotate_scan"
    ) -> dict:
        """
        策略選項：
        - rotate_scan: 原地旋轉 360 度
        - patrol_points: 移動到預設巡邏點
        """
        for attempt in range(max_attempts):
            result = await find_object(target)
            if result["found"]:
                return result

            if strategy == "rotate_scan":
                await self.rotate(angle=60)  # 旋轉 60 度

            await asyncio.sleep(1.0)  # 等待穩定

        return {"success": False, "message": "搜尋失敗"}
```

**新增 MCP Tool：**

```python
@mcp.tool()
async def find_object_with_exploration(
    target: str,
    max_attempts: int = 5,
    exploration_strategy: str = "rotate_scan"
) -> dict:
    """主動尋物（含探索）"""
    manager = ActiveSearchManager()
    return await manager.search_with_exploration(
        target, max_attempts, exploration_strategy
    )
```

---

### 方案 B：物件校正表 🟡 建議做

**核心思想：** 不同物件使用不同的校正參數

**校正表設計：**

```python
# 修改檔案：GPU Server 上的 perception_server_v2.py

CALIBRATION_TABLE = {
    'chair': {
        'thresholds': [0.80, 3.00],
        'scales': [0.66, 0.23, 0.20],
        'blend_width': 0.20
    },
    'bottle': {
        'thresholds': [0.60, 2.50],
        'scales': [0.55, 0.20, 0.18],  # 待測量
        'blend_width': 0.15
    },
    'cup': {
        'thresholds': [0.50, 2.00],
        'scales': [0.50, 0.18, 0.15],  # 待測量
        'blend_width': 0.15
    },
    'bag': {
        'thresholds': [0.70, 2.80],
        'scales': [0.45, 0.18, 0.15],  # 待測量
        'blend_width': 0.20
    },
    'default': {  # 未知物件使用泛化參數
        'thresholds': [0.70, 2.50],
        'scales': [0.55, 0.20, 0.18],
        'blend_width': 0.20
    }
}

def calibrate_depth(depth_raw: float, object_label: str) -> float:
    """根據物件類型進行深度校正"""
    params = CALIBRATION_TABLE.get(object_label, CALIBRATION_TABLE['default'])
    thresholds = params['thresholds']
    scales = params['scales']
    blend_width = params['blend_width']

    # 分段線性校正（平滑過渡）
    if depth_raw < thresholds[0] - blend_width:
        scale = scales[0]
    elif depth_raw < thresholds[0] + blend_width:
        blend = (depth_raw - (thresholds[0] - blend_width)) / (2 * blend_width)
        scale = scales[0] * (1 - blend) + scales[1] * blend
    elif depth_raw < thresholds[1] - blend_width:
        scale = scales[1]
    elif depth_raw < thresholds[1] + blend_width:
        blend = (depth_raw - (thresholds[1] - blend_width)) / (2 * blend_width)
        scale = scales[1] * (1 - blend) + scales[2] * blend
    else:
        scale = scales[2]

    return depth_raw * scale
```

**資料收集計畫：**

| 物件 | 測試距離 | 測試次數 | 預估時間 |
|------|---------|---------|---------|
| chair | 30cm, 45cm, 1m | 已完成 ✅ | - |
| bottle | 30cm, 50cm, 1m | 各 3 次 | 30 min |
| cup | 30cm, 50cm, 80cm | 各 3 次 | 30 min |
| bag | 40cm, 70cm, 1m | 各 3 次 | 30 min |
| remote | 30cm, 50cm, 80cm | 各 3 次 | 30 min |

**測試流程：**
1. 放置物件在 30cm / 50cm / 1m
2. 執行 `python calibration_script.py --object bottle --distances 0.3,0.5,1.0`
3. 分析誤差，調整 scales 參數
4. 重複直到誤差 < 10%

---

## 📁 需要修改的檔案

### Phase 1：主動探索模式

| 檔案 | 位置 | 修改內容 | 程式碼量 |
|------|------|---------|---------|
| `active_search.py` | ros-mcp-server/ | 新增 ActiveSearchManager | ~200 行 |
| `server.py` | ros-mcp-server/ | 新增 MCP Tool | ~80 行 |
| `test_active_search.py` | ros-mcp-server/ | 測試腳本 | ~100 行 |

### Phase 2：物件校正表

| 檔案 | 位置 | 修改內容 | 程式碼量 |
|------|------|---------|---------|
| `perception_server_v2.py` | GPU Server | 新增 CALIBRATION_TABLE | ~80 行 |
| `calibration_script.py` | GPU Server | 資料收集腳本 | ~150 行 |

---

## 📋 待辦事項

### Day 1（12/24）：主動探索基礎 🔴 必做

- [ ] 建立 `active_search.py` 模組
- [ ] 實作 `rotate_scan` 策略（原地旋轉 360 度）
- [ ] 整合到 MCP Tool
- [ ] 測試旋轉搜尋（找椅子）
- [ ] 調整旋轉角度、等待時間參數

**驗收標準：**
- ✅ 椅子在側面或後方時，能透過旋轉找到
- ✅ 最多 6 次嘗試（360° / 60° = 6）

### Day 2（12/25）：校正表資料收集 🟡 建議做

- [ ] 修改 `perception_server_v2.py`，新增 CALIBRATION_TABLE
- [ ] 建立 `calibration_script.py`
- [ ] 測試 bottle（30cm, 50cm, 1m）
- [ ] 測試 cup（30cm, 50cm, 80cm）
- [ ] 分析誤差，調整參數

**驗收標準：**
- ✅ bottle 誤差 < 15%
- ✅ cup 誤差 < 15%

### Day 3（12/26）：整合測試 🔵 選做

- [ ] 測試 bag 和 remote
- [ ] 實作 `patrol_points` 策略
- [ ] 端到端測試：找椅子 + 找水瓶
- [ ] 錄製 Demo 影片

---

## 🔧 技術細節

### 旋轉搜尋邏輯

```python
async def rotate_scan(self, target: str, angle_step: int = 60):
    """原地旋轉 360 度搜尋"""
    
    total_rotation = 0
    max_rotation = 360
    
    while total_rotation < max_rotation:
        # 1. 當前角度拍照
        result = await find_object(target)
        if result["found"]:
            return result
        
        # 2. 旋轉到下一個角度
        await self.publish_cmd_vel(
            linear_x=0.0,
            angular_z=0.3,  # 正值 = 逆時針
            duration=angle_step / 60.0  # 60°/s → 持續時間
        )
        
        total_rotation += angle_step
        await asyncio.sleep(1.0)  # 等待穩定
    
    return {"success": False, "message": "360° 搜尋完成，未找到"}
```

### 速度指令發送

```python
async def publish_cmd_vel(self, linear_x: float, angular_z: float, duration: float):
    """發送速度指令（定時）"""
    
    # 方案 1：使用現有的 move_for_duration service
    service_msg = {
        "op": "call_service",
        "service": "/move_for_duration",
        "type": "go2_interfaces/srv/MoveForDuration",
        "args": {
            "linear_x": linear_x,
            "angular_z": angular_z,
            "duration": duration
        }
    }
    await ws_manager.request(service_msg, timeout=duration + 2.0)
    
    # 方案 2（備案）：直接 publish /cmd_vel + sleep
    # twist_msg = {"linear": {"x": linear_x}, "angular": {"z": angular_z}}
    # await publish_once("/cmd_vel", "geometry_msgs/Twist", twist_msg)
    # await asyncio.sleep(duration)
    # await publish_once("/cmd_vel", "geometry_msgs/Twist", {"linear": {}, "angular": {}})
```

### 依賴服務

- `/move_for_duration`（已存在）：定時移動
- `/cmd_vel`（已存在）：速度控制

---

## 📊 預期成果

### 完成後的尋物流程

```
使用者：「找遙控器」
    ↓
find_object_with_exploration(target='remote', max_attempts=6)
    ↓
Attempt 1: 正前方拍照 → 未找到
Attempt 2: 旋轉 60° → 拍照 → 未找到
Attempt 3: 旋轉 60° → 拍照 → 未找到
Attempt 4: 旋轉 60° → 拍照 → ✅ 找到！
    ↓
回傳：{
  "found": true,
  "label": "remote",
  "distance_m": 0.78,  # 使用 remote 專用校正參數
  "direction": "左側",
  "attempts": 4,
  "total_rotation": 180
}
```

### 改進指標

| 指標 | 改進前 | 改進後 | 提升 |
|------|--------|--------|------|
| 障礙物後搜尋 | ❌ 不支援 | ✅ 支援（360° 掃描）| ∞ |
| chair 誤差 | 4.4% | 4.4%（維持）| - |
| bottle 誤差 | 未知 | < 15%（目標）| - |
| bag 誤差 | 65% | < 20%（目標）| 69% |
| 搜尋成功率 | ~30% | > 80%（目標）| 167% |

---

## ⚠️ 風險與備案

| 風險 | 等級 | 緩解措施 | 備案 |
|------|------|---------|------|
| 旋轉角度不準 | 🟡 中 | 測量實際角速度 | 使用 IMU 監控 |
| 旋轉不穩定 | 🟡 中 | 降低角速度、增加穩定時間 | 使用 Nav2 定點旋轉 |
| 校正參數難調 | 🟡 中 | 自動化測試腳本、多次測量 | 使用單一泛化參數 |
| 時間不足 | 🔴 高 | 優先完成 chair + bottle | 1/7 只展示椅子 |
| GPU Server 連線 | 🟡 中 | 本地快取結果 | 使用預錄影片 |

---

## 🎯 1/7 Demo 建議

**場景：** 找椅子（已驗證，最穩定）

**腳本：**
1. 椅子放在 Go2 側面或後方（視野外）
2. 使用者：「Go2，幫我找椅子」
3. Go2 原地旋轉搜尋 → 找到椅子
4. Go2 走向椅子 → 執行 Hello 動作
5. TTS：「我找到椅子了！」

**備案：** 若主動探索未完成，使用原始 find_object（椅子放在正前方）

---

## 🔗 相關資源

- [Perception Server V2](file:///home/roy422/Yoloworld/perception_server_v2.py)
- [ros-mcp-server](../../ros-mcp-server/)
- [昨日進度 (12/21)](./2025-12-21-dev.md)
- [專題目標](../../00-overview/專題目標.md)
- [開發計畫](../../00-overview/開發計畫.md)

---

**筆記建立時間：** 2025-12-23 19:18  
**預計開始執行：** 2025-12-24  
**預計完成：** 2025-12-26
