# 今日開發計畫 - 2025/12/06

**目標：** 完成 MCP 基礎環境建置，驗證 rosbridge 連線

---

## ⚠️ 重要：VM 網路環境說明（來自 2025/12/01 除錯經驗）

你的 VM 有**雙網卡**，必須正確配置 CycloneDDS：

| 網卡 | IP | 用途 |
|------|-----|------|
| `lo` | 127.0.0.1 | **本機 ROS2 通訊（必須用這個！）** |
| `enp0s1` | 192.168.12.222 | 連接 Go2 機器狗 |
| `enp0s2` | 192.168.1.125 | 連接家用網路（Windows 在這裡） |

**關鍵設定：** 使用 `/home/roy422/local_only_v2.xml` 強制本機通訊走 loopback！

```bash
# 每個 Terminal 都必須設定！
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
export CYCLONEDDS_URI=/home/roy422/local_only_v2.xml
```

---

## 📍 當前狀態

- [x] ros-mcp-server Clone 完成
- [x] 專題目標.md / 開發計畫.md 更新完成
- [x] CycloneDDS 雙網卡問題已解決（2025/12/01）
- [x] rosbridge_server 安裝 ✅ (v2.0.4)
- [x] MCP 連線測試 ✅ (4 topics found)
- [x] ros-mcp-server 安裝 ✅ (66 packages)
- [ ] **Phase 4: Go2 整合測試**（下一步）

---

## 🎯 Phase 1-3 執行記錄

### Phase 1：VM 端環境建置 ✅

- [x] **1.1 安裝 rosbridge_server**
  ```bash
  sudo apt install ros-humble-rosbridge-server -y
  # 結果：ros-humble-rosbridge-server (2.0.4) 升級成功
  ```

- [x] **1.2 確認 CycloneDDS 配置存在**
  ```bash
  cat /home/roy422/local_only_v2.xml
  # 確認包含 <NetworkInterface name="lo"/>
  ```

- [x] **1.3 啟動 rosbridge**
  ```bash
  source /opt/ros/humble/setup.zsh
  export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
  export CYCLONEDDS_URI=/home/roy422/local_only_v2.xml
  ros2 launch rosbridge_server rosbridge_websocket_launch.xml
  # 結果：Rosbridge WebSocket server started on port 9090
  ```

- [x] **1.4 確認 rosbridge 啟動成功**
  ```bash
  ss -tlnp | grep 9090
  # 結果：LISTEN 0.0.0.0:9090 (python3, pid=93858)
  ```

### Phase 2：連線測試 ✅

- [x] **2.1 安裝 websockets Python 模組**
  ```bash
  sudo pip3 install websockets
  # 結果：Successfully installed websockets-15.0.1
  ```

- [x] **2.2 本機測試 WebSocket 連線**
  ```python
  # 使用 Python 測試 rosbridge
  async with websockets.connect('ws://127.0.0.1:9090') as ws:
      await ws.send(json.dumps({'op': 'call_service', 'service': '/rosapi/topics'}))
  # 結果：✅ 連線成功，發現 4 個 topics
  #   - /client_count
  #   - /connected_clients
  #   - /parameter_events
  #   - /rosout
  ```

### Phase 3：ros-mcp-server 測試 ✅

- [x] **3.1 確認 uv 已安裝**
  ```bash
  which uv
  # 結果：/home/roy422/.local/bin/uv
  ```

- [x] **3.2 測試 ros-mcp 基本指令**
  ```bash
  uvx ros-mcp --help
  # 結果：安裝 66 packages，顯示 help 訊息
  # 支援 transport: stdio, http, streamable-http, sse
  ```

---

## 🎯 Phase 4：Go2 整合測試（待執行，需開機 Go2）

- [ ] **4.1 Terminal 1: 啟動 Go2 Driver**
  ```bash
  cd ~/ros2_ws/src/elder_and_dog
  source install/setup.zsh
  export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
  export CYCLONEDDS_URI=/home/roy422/local_only_v2.xml
  
  ros2 launch go2_robot_sdk robot.launch.py slam:=false nav2:=false
  ```

- [ ] **4.2 Terminal 2: rosbridge 已在運行中**
  ```
  PID: 93858, Port: 9090
  ```

- [ ] **4.3 驗證 rosbridge 能看到 Go2 Topics**

- [ ] **4.4 測試基本移動（透過 MCP）**

---

## ⚠️ 已知問題與解決方案

| 問題 | 原因 | 解決方案 |
|------|------|---------|
| **Terminal 間看不到 topics** | CycloneDDS 走錯網卡 | 確保所有 Terminal 都設定 `CYCLONEDDS_URI=/home/roy422/local_only_v2.xml` |
| **ros2 node list 可看到，但 topic hz 無數據** | Discovery 成功但 Data Transfer 失敗 | 使用 loopback 配置 |
| **Port 9090 無法從 Windows 存取** | 防火牆或網路配置 | 確認 enp0s2 (192.168.1.125) 可 ping 通 |
| **uvx 找不到** | 未載入環境 | `source ~/.bashrc` 或重開 Terminal |

---

## 📊 今日成功標準

- [x] ✅ rosbridge 在 VM 上穩定運行（Port 9090）
- [x] ✅ 在 VM 本機用 Python websockets 能連線
- [ ] ⏳ 從 Windows 用 `wscat` 能連線到 VM
- [ ] ⏳ ros-mcp-server 能列出 Go2 的 Topics
- [ ] ⏳ 能透過 MCP 發送測試指令

---

## 📝 筆記區

```
- 11:44 開始執行 Phase 1
- 11:45 rosbridge_server 安裝完成 (v2.0.4)
- 11:47 rosbridge 啟動成功 (Port 9090)
- 11:50 websockets Python 模組安裝，測試連線成功
- 11:52 uvx ros-mcp --help 成功，66 packages installed
- 11:56 Phase 1-3 完成，準備 Phase 4 (需開機 Go2)

rosbridge 背景運行中：PID 93858
```
