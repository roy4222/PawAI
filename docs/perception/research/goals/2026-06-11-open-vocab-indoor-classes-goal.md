# Research Goal: 居家類別擴充 — open-vocabulary vs 更大 closed-set（COCO 80 不夠用）

> Generated by `goal-research` skill on 2026-06-11.
> Spec mode: multi-goal member 2/4.

## Bottleneck being broken

COCO 80 類對居家室內機器狗太少（藥瓶、遙控器、眼鏡、拐杖、鑰匙、碗筷…全抓不到，「抓的範圍太少」）。要決定走哪條路擴類別——open-vocabulary（text prompt）還是更大 closed-set（LVIS 級）——以及這條路在 Orin Nano 8GB 上的真實成本，否則類別擴充永遠停在許願。

This goal exists to produce a falsifiable verdict on this specific bottleneck. If the result reads like a literature review without a verdict, the skill failed.

## Context

- Project: 老人與狗 / PawAI（居家互動 70% / 守護 30%）
- Hardware / stack: Jetson Orin Nano 8GB、JetPack 6、onnxruntime-gpu 1.23.0 + TRT EP FP16；現役 YOLO26n COCO80、`class_whitelist` 參數已支援子集過濾
- Deadline: 下次 Jetson 上機測試日
- Hard constraints: ultralytics 禁裝 Jetson（WSL export only）；RAM ≥0.8GB 餘量；事件 schema `/event/object_detected` 的 `label` 欄位是自由字串（擴類別不動 contract）
- Related docs already in repo (cross-validate against these):
  - `docs/contracts/interaction_contract.md`（object event schema）
  - `docs/perception/research/2026-06-11-pinto-model-zoo-pawai-fit-report.md`（zoo 484 目錄裡 open-vocab 供給情況——盤點時未被任何角度撈出，需確認是真缺席還是漏掃）
  - `object_perception/object_perception/coco_classes.py`（現役 80 類表）

## Sources to read end-to-end

1. https://docs.ultralytics.com/models/yoloe/ — YOLOE（prompt-free / text-prompt open-vocab）：**關鍵機制 = `set_classes()` 後可 export 成固定 vocabulary 的普通 detector**（runtime 零 text-encoder 成本）；確認 YOLO26 世代是否有對應版本
2. https://docs.ultralytics.com/models/yolo-world/ — YOLO-World 同款 set-vocab-then-export 機制與尺寸/精度表
3. LVIS 1203 類 closed-set 路線：WebSearch `LVIS trained detector ONNX edge deployment`（找可直接 export 的權重）
4. Grounding DINO / OWL-ViT 級的反面證據：WebSearch `Grounding DINO Jetson Orin Nano fps`（預期太重，要有數字佐證出局）
5. 本地 `$HOME/newLife/PINTO_model_zoo`：`grep -ril "yolo-world\|yoloe\|open.vocab\|grounding" */README.md` 確認 zoo 供給
6. 居家長者場景類別需求：從 `docs/mission/README.md` 與 demo 腳本（S1-S5）反推目標類別清單

For each source, cite specific evidence — claims without citations are rejected.

## Required investigation

- 先列「PawAI 居家目標類別清單 v0」：從互動場景（拿杯子、藥瓶提醒、遙控器尋物）與 guardian 場景（拐杖、輪椅——注意 PINTO 報告已有 Wholebody 系覆蓋部分）反推 20-40 類，標注哪些在 COCO 80 內、哪些不在
- YOLOE / YOLO-World 的「set vocabulary → export 固定 head」流程細節：WSL 上可行性、export 後輸出格式是否仍 NMS-free `(1,300,6)` 同形態（整合成本）、自訂 vocab 的 zero-shot 精度對「沒見過的居家小物」實際多可靠（找 LVIS-rare AP 數字當 proxy）
- 兩條路線的 Jetson 成本：YOLOE-26s（或對應尺寸）export 後 vs 現役 n 的 GFLOPs/RAM 差距
- closed-set 備選：有沒有現成 LVIS/Objects365 訓練、可 ONNX export、Jetson 可負擔的模型（含 PINTO zoo 內）
- 反面案例固定：Grounding DINO 級 transformer open-vocab 在 Orin Nano 的 FPS 數字（預期 <2，引用後正式出局，避免之後再被提起）
- 風險面：open-vocab 對相似類的混淆（藥瓶 vs 水瓶 vs 杯子）、中文場景詞→英文 prompt 的對應表維護成本
- 類別擴充與 `class_whitelist`/brain gate 的連動：新類別進來後 brain 端誰消費（目前 S3 只認 cup）——標注這是 scope 外但要記錄
- 與 goal 1 的耦合：如果 goal 1 結論是升 s@960，open-vocab 模型也要同尺寸比較才公平

## Specific questions (citation required for each answer)

- Q1: YOLOE（或 YOLO26 世代等價物）的 set-classes-then-export 流程是什麼？export 產物在 onnxruntime TRT EP 上與普通 YOLO26 ONNX 有無差異？
- Q2: YOLOE/YOLO-World 各尺寸的 LVIS AP（特別是 rare 類）與 params/GFLOPs 表？
- Q3: 自訂 30 類居家 vocab export 後，對「訓練分佈外的小物」（藥瓶、遙控器）的可靠度有什麼可引用的證據？
- Q4: PawAI 居家類別清單 v0 是哪些？COCO 80 覆蓋率多少 %？
- Q5: PINTO zoo 裡有沒有 open-vocab 或 LVIS 級供給？（目錄名+README 證據；沒有也要明說）
- Q6: Grounding DINO 在 Orin Nano 的實測 FPS？（出局證據）
- Q7: open-vocab export 模型在 Orin Nano 的 RAM/FPS 估算 vs 現役 n@640？
- Q8: 若走 open-vocab，上機測試該測什麼？（vocab 清單、測試物件組、recall 門檻）
- Q9: 兩條路線哪條能與 goal 1 的勝出配置疊加（同一顆模型同時解尺寸與類別）？

## Verdict slot (REQUIRED — must pick exactly one)

Pick one of:
- `GO_YOLOE_CUSTOM_VOCAB` — open-vocab set-then-export 路線可行，附 vocab 清單 v0 + WSL export 步驟 + 上機驗收項
- `GO_EXPANDED_CLOSED_SET` — 指名一個 LVIS/大類別 closed-set 模型進 benchmark（附來源與成本估算）
- `NO_GO_STAY_COCO80` — 兩條路成本都過不了 8GB/Hz 門檻，或精度證據不足；留 COCO80 + class_whitelist
- `NEEDS_TEST_VOCAB_REPLAY` — 紙面精度證據不足，需先 WSL 對 demo 錄影/自拍居家物件照離線測 vocab 命中率再決定

If no enumerated option fits the evidence, the verdict is `NEEDS_NEW_RESEARCH` with explicit description of what new research is needed.

## Cross-validate against

- `docs/perception/research/2026-06-11-pinto-model-zoo-pawai-fit-report.md`（§3 表：object 線「此 zoo 無解」是針對 cup 遠距，不是類別擴充——別混淆兩個結論）
- `docs/mission/README.md`（互動 70%/守護 30% 定位——類別清單要對齊場景而非貪多）
- `docs/contracts/interaction_contract.md`

State explicitly when this research's finding contradicts a prior doc.

## Constraints

- Read-only audit. No code changes. No git commits. No installs. Hardware untouched.

## Done criteria

- ≥ 30 specific findings with citations
- All Q1–Q9 answered with citation per claim
- Verdict slot filled with exactly one enumerated value
- One concrete actionable next step matching the verdict
- Saved at `docs/perception/research/2026-06-11-open-vocab-indoor-classes-result.md`
